;-------------------------------------------------------------;- SyncIt!;- small tool to synchronize two directories;- include file: synchronize.pbi;- Copyright 2020 by Markus Mueller <markus.mueller.73 at hotmail dot de>;- This program is free software;-------------------------------------------------------------Procedure sync_dirs( List l._FILE_DESCRIPTOR_EX() )    info("syncronizing directories.")    ForEach l()    If l()\to_copy      info("copying: " + l()\path\src + l()\name + " to " + l()\path\dst)    EndIf  Next    info("synchronizing done.")  EndProcedure; IDE Options = PureBasic 6.02 LTS (Windows - x64); CursorPosition = 8; Folding = -; Optimizer; EnableXP; EnablePurifier; EnableCompileCount = 0; EnableBuildCount = 0; EnableExeConstant